{
  "name": "@collaboration-operations-platform/search",
  "version": "0.1.0",
  "type": "module",
  "description": "Policy-safe search document projection and in-memory indexing pipeline.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --import tsx --test tests/search.test.ts"
  }
}
