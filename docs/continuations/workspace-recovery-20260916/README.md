# Workspace recovery: stopped session, unfinished outcome

The user requested closeout after rejecting continued audit/fix cycling. This capsule preserves the full original recovery scope; it does not authorize automatic continuation or declare recovery complete.

Owner/checklist: https://github.com/4444J99/limen/pull/2673
Managed configuration owner: https://github.com/organvm/domus-genoma/pull/390
Evidence: ../../receipts/workspace-recovery-20260916.md and session-closeout.json here.

## Intent and boundaries

Work from the single PR2673 checklist. Keep each original requirement open until operational evidence proves it. Do not replace the goal with repository greenness, another census, or more folders. Preserve dirty/untracked/stashed/private work in its existing custody. No mass relocation, automatic issue campaigns, new scheduler, or competing registry. Session closeout is not implementation completion.

The existing isolated recovery checkout is retained as the successor workspace, avoiding another worktree. Its tracked branch is work/workspace-recovery-20260916. No agent is launched by preparation or verification. Resume only after explicit user direction; do not reset cumulative budget, retry count, or existing outcome identity.

## First action on an explicitly requested resume

Read the live checklist, containment policy and execution-budget owner before mutations. Pick one unchecked deliverable and finish its implementation/deployment/acceptance before moving to another. Record any necessary exception and its owning next action in that same checklist. Refresh evidence only when it changes the next action.

Latest interrupted item: 21 old immutable runtimes were audited but NOT removed. All 41,788 exported source files matched recorded Git archives; all installed Limen package files matched their receipt-bound wheels; all source commits are ancestors of deployed main. One runtime contains an additional notification-canary receipt. Preserve that receipt with its existing owner before any exact-plan prune. The native runtime pruner only checks receipt shape/process ownership; the completed content audit is additional required evidence. Recheck current ownership and exact candidate identity before removal.

## Open work and owners

- Limen recovery owner: correct historical merge-path gate expansion (origin/main selected 57 already-landed paths; HEAD-relative closeout selected four gates), finish estate producer coverage, live approved-priority/concurrency/restart acceptance, successful fanout recovery-clone retirement, complete local identity/custody exceptions and per-ref purposes, retained chamber/stash custody, final counts and idempotence.
- Domus runtime owner: finish custody of the extra canary receipt, then apply a freshly verified exact runtime-prune plan; retain current/active/changed entries.
- Domus PR390: changes are merged/deployed. Its companion checkout is retained pending ignored-cache disposition and full process/session ownership clearance. No tracked/untracked implementation changes remain there.

## Verification and launch

`python3 docs/continuations/workspace-recovery-20260916/verify-closeout.py` checks this handoff's custody and containment only. It MUST NOT be used as a recovery-completion predicate.

To inspect the prepared continuation without launching an agent:
`bash docs/continuations/workspace-recovery-20260916/launch.sh --check`

On explicit user resumption, the same launcher without --check delegates to the existing private capsule kickstart and its original finite contract. It never creates another checkout. An expired contract stops launch; it is not silently renewed. The public receipt and the existing PR are the durable entry points if local preparation must be reconstructed.
