<!-- GENERATED by organs/consulting/constellation/derive-streams.py — hand-edits outside the
     operator-notes block are drift (check-session-streams.py check M). Change the register, rerun. -->

# dustin — collaboration domain

**Tier T2** (operator-accepted 2026-07-22) · **3 lanes** ·
owner of record `organs/consulting/constellation/registry.yaml`.

## Objective

This stream owns dustin's WHOLE collaboration — every lane below lives in this one session.
Never open a sibling session for a single lane of this domain. Work the lanes in the order
listed (register order); each lane's stage objective is stated in its section, and the session's
increment is the sum of the lanes it advanced, named per lane in the closeout.

Engagement: `organs/consulting/engagements/dustin.yaml` · Funnel instance: none

## Lanes (from the register — verify before acting; re-derive, never trust this file's memory)

### dsp-platform — stage **building**, public face **none**

> Register note: consumer shell still gated on ten design questions sent 2026-03-28 and never answered; he is the correct respondent (working musician + practising UI/UX). Stage-1 coordination substrate built 2026-08-12 — split sheets, settlement (pro-rata vs user-centric with the delta computed), provenance attestation, delivery validation; 59 tests, no network, no persistence. Research corpus was Drive-only until 2026-08-13, now mirrored into the repo's docs/research/. Alpha→omega roadmap issued 2026-08-13: 11 phases as GitHub milestones + issues, ladder at roadmap/ladder.json, consumer-shell phases planned-not- started behind L-DSP-TEN-QUESTIONS.

Advance the build to its next verifiable rung. Read the repo's own issues/PRs/README for the current edge; leave the lane one demonstrable increment further with the increment stated in the closeout.

Repos:
- `4444J99/post-dsp-platform`

Keywords: dsp, music platform, post-dsp, streaming alternative · Corpus evidence:
`python3 scripts/constellation-dossier.py --slug dustin --project dsp-platform`

### ai-consultation — stage **building**, public face **none**

> Register note: entry point and segment were already chosen in the repo's own README (AI Estate Audit, higher education, fixed scope); only PRICE is open, and it does not gate the instrument. Audit instrument built 2026-08-12 — estate schema, legibility rubric where every lost point names a missing artifact, derived risk register, capped first wave; 66 tests. His enterprise-UX vantage is the credibility asset, not delivery capacity. Alpha→omega roadmap issued 2026-08-13: 10 phases as GitHub milestones + issues, ladder at roadmap/ladder.json; only the commercial-packaging phase waits on L-CONS-PRICE.

Advance the build to its next verifiable rung. Read the repo's own issues/PRs/README for the current edge; leave the lane one demonstrable increment further with the increment stated in the closeout.

Repos:
- `4444J99/the-consulate`

Keywords: ai audit, ai consultation, ai consulting, consulate, ai remediation, ai governance · Corpus evidence:
`python3 scripts/constellation-dossier.py --slug dustin --project ai-consultation`

### social-presence — stage **building**, public face **none**

> Register note: independent-musician instance of the content yield engine; inbound PAID request 2026-08-06, unanswered. Hard scope constraint set by him: no generative filler — now enforced in code by the instance repo's caption guard, not by prose. First working code landed 2026-08-12 (release-cycle planner, 38 tests); nothing in it posts, and terms remain unagreed. Alpha→omega roadmap issued 2026-08-13: 10 phases as GitHub milestones + issues, ladder at roadmap/ladder.json; offer and posting phases planned-not-started behind L-NAS-TERMS / L-NAS-CREDENTIALS.

Advance the build to its next verifiable rung. Read the repo's own issues/PRs/README for the current edge; leave the lane one demonstrable increment further with the increment stated in the closeout.

Repos:
- `4444J99/new-ancients-social`

Keywords: social presence, new ancients, ghastly occurrences, shorts campaign, music promotion, release cycle · Corpus evidence:
`python3 scripts/constellation-dossier.py --slug dustin --project social-presence`

## Evidence, not recall

- Board state: `limen progress --view workstream --scope consulting` and
  `grep -n "CONST-" tasks.yaml` — the board is live; this cartridge deliberately embeds none of it.
- Dossier commands are per lane, above (dry-run report; `--write` to emit dossier files).
  **Never paste corpus/atom statement text into this repository — it is public.**
- The person context beyond first-name slug lives in the private overlay; reference it by path
  only, never copy content into the public tree.

## Fan-out

At most **4** children, only via `limen conduct split <parent_run> --packet`, which reserves
each child against this session's lineage before launch. Never nest a git worktree inside this
one — the reclaim organ sweeps roots, so a nested worktree leaks. Tier every child explicitly.

## Constraints

Fresh branch `feat/dustin-<increment>` off updated `origin/main`, one concern per branch.
`scripts/verify-scoped.sh` as the push gate; `merge-policy.sh` → `await-pr.sh <PR#> --merge`.
**A domain never settles by trailer** — it is recurring work you will reopen; a `Settles:` claim
is ignored for constellation rows. The domain leaves the launcher when the OPERATOR re-tiers or
removes the person in `organs/consulting/constellation/registry.yaml` (then rerun
`derive-streams.py --write`). Record a lane's stage change the same way: register first,
derivation follows.

## Done

The objective above is delivered with a verifiable increment named per advanced lane in the
closeout, and `organs/consulting/constellation/constellation-done.sh` plus
`python3 organs/consulting/constellation/validate-constellation.py` stay green. If the register's
stage for any lane changed as a result, update `registry.yaml` and rerun
`python3 organs/consulting/constellation/derive-streams.py --write` in the same branch.

<!-- operator-notes:begin -->

<!-- operator-notes:end -->
