# Current evidence and next checks

The successor was created from exact remote-custodied predecessor head
`ae07414880a64dac44e6ad56e2ced4e2394089d4`. Its unstarted v3 contract records a 90-minute runway,
`danger-full-access`, dynamic provider/model selection, and the normal retained high-risk gates. No
provider run exists yet.

At launch, the generated kickstart must validate live Codex support, fetch and inspect the worktree,
register the direct session through conduct with `--human-protected`, prove that registration, and
only then admit the runway and publish its receipt. Failure at any pre-admission boundary must leave
the contract unstarted and must not launch Codex.

## Immediate host defect — owner Limen #2664

The installed background-items census calls `sfltool dumpbtm` automatically in `build_report`, and
the operator observed an administrator dialog. Repair the scheduled census so unattended collection
cannot invoke a modal privileged path. Preserve LaunchAgents classification, report missing BTM
measurement explicitly, and keep a supported intentional BTM inspection path. Verify source,
deployment, and runtime receipts separately.

Re-read live default, CI, installed runtime receipt, and launchd state before acting. Do not fire
extra probes merely to accelerate scheduled adoption. Broker state owns tasks, claims, and receipts;
the capsule's bypass profile is not authority to widen a child packet or mutate a human gate.
