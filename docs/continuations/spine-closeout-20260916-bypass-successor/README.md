# Spine-first bypass-all successor

Read `intent.md`, `runtime.md`, `closeout.md`, and `workstream.json` in that order. This is a
bounded continuation of unfinished estate work, not an estate-green or strict-closeout receipt.

## Launch

From this worktree:

```bash
bash docs/continuations/spine-closeout-20260916-bypass-successor/launch.sh
```

Use `--check` to validate without registering a session, admitting runway, or launching Codex.
The tracked v3 receipt binds this direct successor to `danger-full-access`; the generated launcher
must complete human-protected conduct registration before admission and then passes Codex the one
exact `--dangerously-bypass-approvals-and-sandbox` flag. Provider and model selection remain live.

The 90-minute runway begins on actual launch. The private capsule is ignored local state; its
tracked receipt and this wrapper are the durable handoff. Do not handcraft the private identity or
contract files.

## Evidence boundary

- Owner: https://github.com/4444J99/limen/issues/2664
- Launcher correction: `ae07414880a64dac44e6ad56e2ced4e2394089d4`, pushed to
  `origin/work/spine-closeout-20260916` before successor creation.
- Focused contract/launcher suite: 99 tests passed before the protected-registration hardening;
  the three changed protected-registration regressions passed afterward.
- The exact-tree scoped gate passed its complete cheap wave. Heavy verification was denied by the
  machine-wide `swap-fraction` admission guard and therefore is not claimed as run or green.
- Successor creation did not launch a provider, admit runway, merge, deploy, or establish estate
  completion.
