Program: https://github.com/4444J99/limen/issues/2739

## Owner and deliverable

Domus owns host configuration, native adapters and installed runtimes. Make control capabilities and agent history independent of editable Limen, Domus and PORTVS source so those checkouts can become remote-default.

## Known baseline

The heartbeat already runs from a receipt-bound immutable runtime. Native agent roots and private context still depend on locations inside the Limen checkout; chezmoi still uses an editable Domus source. Preserve active clients and unrelated source edits throughout cutover.

## Acceptance

- Enumerate actual installed, loaded, executable, config and native-session consumers from their existing owners.
- Derive source/runtime/state/custody roots through Domus; change templates/generators rather than deployed files.
- Preserve and verify unique state before scoped cutover. Drain/restart only the affected clients through the existing native lifecycle; do not interrupt protected sessions.
- Fresh native-client and natural scheduled canaries succeed using installed capabilities with no dependency on editable control checkouts.
- Verify rollback and retained source/custody evidence before the owning reclaimer retires the old checkouts.
- Keep secrets with their native credential authority; no bulk home relocation.

## Related work

Existing Domus #320, #331, #332, #334 and #359 remain separately owned. Limen shared-root history: https://github.com/4444J99/limen/issues/1366. Refresh stale observations without overwriting history. E1/E5 supply required custody; this epic is the only necessary control-source detachment prerequisite for E2.

## Standing constraints

Authorized directly by the user on 2026-09-25. Remote availability comes first; housekeeping must remain bounded and support active PRDS work. Preserve all current PRDS-related repositories and active worktrees. Preserve private originals, snapshots, unique refs/stashes/metadata and active-owner state until their applicable custody gates pass. No new paid service, blanket deletion, forced main update, or automatic activation of unavailable providers.

Public receipts contain neutral identifiers and aggregates. Private inventories and sensitive content stay with their authorized owners. A push, issue, test or archive alone is not proof of complete recovery. This epic starts open; existing evidence is credited only to its actual scope.
