# Agy autonomous intent — Collaboration Operations Platform Alpha to Omega

Conduct and execute the complete private Collaboration Operations Platform program without asking
the operator for additional planning, architecture, prioritization, test-scope, branch, cleanup, or
implementation guidance.

## Read first

1. `/Users/4jp/AGENTS.md`
2. repository `AGENTS.md`
3. `.agents/skills/agy_conductor/SKILL.md`
4. `.codex/plans/2026-08-03-collaboration-operations-platform-alpha-omega.md`
5. `docs/continuations/collaboration-operations-platform-alpha-omega-20260803/architecture.md`
6. `docs/continuations/collaboration-operations-platform-alpha-omega-20260803/execution-dag.yaml`
7. `docs/continuations/collaboration-operations-platform-alpha-omega-20260803/acceptance.md`
8. `institutio/collaboration-operations/platform.yaml`

## Authority and retained gates

The direct human instruction dated 2026-08-03 authorizes the private
`organvm-iii-ergon/collaboration-operations-platform` repository genesis and all non-destructive
implementation, testing, private branch/PR, and synthetic-data work described by this plan. Do not
ask for that authorization again.

Retain separate gates for destructive personal-data actions, credential movement or minting, paid
spend, collaborator invitations/transfers, public send/release, DNS, production deployment, and
live client-data or transcript import. A retained gate does not stall independent work: file it once
with owner, failed predicate, and next command, then continue every reversible packet.

## Required opening sequence

1. Run `./scripts/agy-clock.py` and query authenticated `limen conduct capabilities`.
2. Confirm the launcher registered this exact native Agy session as human-protected. Never register
   or impersonate another provider identity.
3. Run `python3 scripts/check-collaboration-operations-plan.py`.
4. Re-probe exact local, topic, remote-default, and target-repository heads plus host admission.
5. Materialize and submit one schema-valid root `WorkPacketV1` whose authority covers only this
   program. Do not mutate before its lease covers the exact worktree, branch, paths, and permitted
   private-repository genesis effect.
6. Read the execution DAG and reserve every child with `limen conduct split` before invoking any
   subagent/team/fleet capacity. Children reduce scope, deadline, spend, depth, and fanout.

## Team topology

- Agy is the native root conductor/integrator, not a master identity.
- Use at most four children and depth one. Derive healthy providers and models live; never encode a
  future model name or substitute another provider under Agy's identity.
- Keep one writer per worktree. Independent architecture, data/policy, experience, connector, and
  verification packets may run in parallel in separate worktrees when host admission allows.
- Require structured child receipts: changed paths, exact heads, predicate, bounded output, spend,
  blockers, and durable custody. Harvest all children before integrating.
- Assign an independent review packet before merging each phase batch.

## Execution behavior

- Treat `execution-dag.yaml` as the work queue and its dependency graph as authoritative.
- Write each phase predicate before its implementation. Preserve green exact-head receipts.
- Mint the target repository only after the discharged lever receipt, fresh remote-absence probe,
  estate shelf, and four genesis gates pass. The founding push is the only direct-main exception.
- After genesis, clone into isolated target-repository worktrees and use topic branches plus PRs.
- Implement the chosen architecture; do not reopen ordinary stack choices as operator questions.
- Use synthetic fixtures exclusively until a source-specific live-data gate is separately satisfied.
- Run focused probes during development and the target repository's scoped resolver once per exact
  tree batch. Do not rerun unchanged green shards.
- Never edit Limen `tasks.yaml`. Submit lifecycle events through the broker.
- Never leak secrets, raw prompts, client content, private paths, or credentials into Git, logs,
  prompts, packets, PRs, or receipts.

## Boundary behavior

At each packet boundary derive `continue`, `switch`, `wait_relay`, `settled`, or `invalid` from live
state. If the finite runway cannot safely hold the next packet, emit and, when broker/host admission
permits, launch a successor capsule carrying this same plan and remaining DAG state. Do not reset the
current capsule deadline or wait for the operator.

Finish only when the target `scripts/omega.sh` passes twice on one unchanged exact head, every DAG
packet has a terminal receipt, every retained gate is owner-routed, all useful work is pushed, and
disposable clean worktrees have remote custody and are reaped through the accepted path.
