<!-- GENERATED by organs/consulting/constellation/derive-streams.py — hand-edits outside the
     operator-notes block are drift (check-session-streams.py check M). Change the register, rerun. -->

# sales-client-engine — rob's lane

**Tier T1** (operator-accepted 2026-07-22) · stage **idea** · public face
**none** · owner of record `organs/consulting/constellation/registry.yaml`.

> Register note: operator-named lane (2026-07-22); brainstorms live in the chat-app corpus — dossier first, build on evidence

## Objective

Dossier first. Assemble the evidence from the corpus (`python3 scripts/constellation-dossier.py --slug rob --project sales-client-engine`), then bring a build/no-build recommendation to the operator's demand review. Do not scaffold a repo before that review — minting is `scripts/repo-genesis.py`'s job and it is evidence-gated.

## Ground (from the register — verify before acting; re-derive, never trust this file's memory)

Repos:
- none minted — evidence gate not yet passed

Keywords: sales engine, client management, crm, sales pipeline, book of business, client tracker, sales os

Engagement: `organs/consulting/engagements/rob.yaml` · Funnel instance: `organs/consulting/funnel/instances/rob-fitness.yaml`

## Evidence, not recall

- Board state: `limen progress --view workstream --scope consulting` and
  `grep -n "CONST-" tasks.yaml` — the board is live; this cartridge deliberately embeds none of it.
- Corpus evidence: `python3 scripts/constellation-dossier.py --slug rob --project sales-client-engine`
  (dry-run report; `--write` to emit dossier files). **Never paste corpus/atom statement text into
  this repository — it is public.**
- The person context beyond first-name slug lives in the private overlay; reference it by path
  only, never copy content into the public tree.

## Fan-out

At most **2** children, only via `limen conduct split <parent_run> --packet`, which reserves
each child against this session's lineage before launch. Never nest a git worktree inside this
one — the reclaim organ sweeps roots, so a nested worktree leaks. Tier every child explicitly.

## Constraints

Fresh branch `feat/sales-client-engine-<increment>` off updated `origin/main`, one concern per branch.
`scripts/verify-scoped.sh` as the push gate; `merge-policy.sh` → `await-pr.sh <PR#> --merge`.
**A lane never settles by trailer** — it is recurring work you will reopen; a `Settles:` claim is
ignored for constellation rows. The lane leaves the launcher when the OPERATOR re-tiers or removes
it in `organs/consulting/constellation/registry.yaml` (then rerun `derive-streams.py --write`).
Record a stage change the same way: register first, derivation follows.

## Done

The objective above is delivered with a verifiable increment named in the closeout, and
`organs/consulting/constellation/constellation-done.sh` plus
`python3 organs/consulting/constellation/validate-constellation.py` stay green. If the register's
stage for this lane changed as a result, update `registry.yaml` and rerun
`python3 organs/consulting/constellation/derive-streams.py --write` in the same branch.

<!-- operator-notes:begin -->

<!-- operator-notes:end -->
