# Workspace storage admission checkpoint

Status: implemented in the recovery branch; not merged or deployed. Owner: workspace-recovery-20260916.

At 2026-09-23 22:40 UTC, `/System/Volumes/Data` had 21,116,665,856 available bytes (about 19.7 GiB; 96% used). The shared local worktree reservation boundary now measures available bytes on the target volume. It latches new disposable worktree creation closed below 50 GiB and remains closed until at least 200 GiB is available. The latch lives under the user's XDG state directory, survives producer restarts, and fails closed if its state cannot be read or written. Existing worktrees are not gated.

The boundary covers Limen's transactional initializer, ordinary dispatch, Jules landing, fanout landing and verification checkouts, audit fixers, cells, ship-docs, and worktree session startup. Paired worktree and branch reservations check storage before any branch reservation where the producer can do so.

Local acceptance at the measured 19.7 GiB returned `storage-headroom-latched-until-200-gib-free` and wrote a blocked latch. Ten focused tests passed across threshold behavior, invalid state, worktree initialization, and unrelated missing worktree registrations. Python compilation, shell syntax checks, and `git diff --check` passed. The broader suite was not run locally. This is source-branch evidence only; it does not prove merge, runtime deployment, cross-repository producer coverage, or free-space recovery.

The exact-SHA reclaimer plan contained two candidates. `gap-acceptance-refresh-20260916` was clean, had no ignored payload, and its head exactly matched the live remote branch. `merge-policy-denial-receipts-20260916` was clean, had no ignored payload, and its head was an ancestor of freshly fetched `origin/main`. A first apply was stopped by an unrelated missing worktree registration (`/private/tmp/wt2607`). The worktree ownership check was corrected to tolerate unrelated missing registrations while revalidating the requested target; the exact same plan then removed both candidates. Branch refs were retained. A second reclaimer check found zero eligible candidates. Available bytes afterward were 21,055,193,088 (about 19.6 GiB); concurrent disk activity means no net volume gain is claimed.

At 23:25 UTC, the generated-cache reclaimer found 68 ignored `node_modules` trees totaling 32,111,087,616 apparent bytes. Its exact-plan apply (`81e6e94ac4fbc8a373e465365d517dbc2e62af6154106d2b5b9d504eaf1150eb`) removed all 68; a fresh scan found none. The tool-cache reclaimer then removed seven idle allowlisted caches totaling 4,797,480 KiB under plan `408d23e280dafd94acbc9dcedbfd36b5657ff191004abd00844f2d15f1339728`; the post-apply scan found no remaining candidates. Internal free space remained about 19.1 GiB after both operations. The internal APFS volume has two purgeable Time Machine snapshots, including the latest snapshot that limits the container minimum. The matching external Time Machine destination is a separate USB device but its APFS volume is unencrypted. Snapshot retirement was held pending the required custody/retention decision. The byte totals are directory totals, not physical bytes freed; APFS snapshot retention explains why `df` did not increase.

The clone reaper found four additional clean remote mirrors, all under 0.01 GB combined, but its separate human acceptance ledger has no entries, so none were removed. Eighty-one other clone paths were retained for active process, core, dirty/untracked content, ignored data, linked worktrees, submodules, or unpushed objects. The broader test gate also exposed a timeout contract defect: `pytest-cli` is registered for 1,500 seconds but the verifier clamped the aggregate batch to 600 seconds and defaulted individual gates to 300 seconds. The verifier aggregate ceiling and all PR-gate invocation paths now allow 1,800/1,700 seconds respectively, with the gate job capped at 40 minutes. Focused verifier hardening and merge-queue contracts pass; remote CI and merge/deployment remain pending.
