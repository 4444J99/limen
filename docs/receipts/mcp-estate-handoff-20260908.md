# MCP-ESTATE-20260908 — requested handoff

## Resume prompt

Continue MCP-ESTATE-20260908 through implementation, deployment and live estate acceptance. The operator explicitly requested this handoff; it is not completion. Read the original plan and `docs/receipts/mcp-estate-20260908.md`, plus global and repository instructions. Preserve every requirement and unaffected verification receipt. Do not stop at another partial handoff while independent admissible work remains. Host admission constrains its implicated heavy lane, not all source implementation.

### Verified remote source state at handoff preparation

All three PRs are open drafts, not merged or deployed:

- Limen #2569, `fix/keeper-publication-recovery-20260908`: `256cc3f5c29a135326ce6f2bbfd65568c958ea2f`.
- Limen #2567, `feat/mcp-estate-contract-20260908`: implementation head `94f011c6dc5c0b8c300ed5145d106f5f16fd5b5b`. This handoff is a subsequent documentation commit on that same branch; resolve its current remote tip rather than resetting to this implementation head.
- Domus #379, `feat/mcp-estate-policy-20260908`: `6f8adbb0f2103adbb10d09ca5633d07356d725fe`.

The feature worktree was clean before this handoff file. It is `.worktrees/mcp-estate-complete-contract-20260908` under Limen. Earlier disposable prerequisite and Domus worktrees were removed after their exact heads were pushed. Preserve unrelated branches, worktrees, processes and stashes.

### Implemented and verified increments

1. Prerequisite fixtures: deterministic diurnal receipt dates and canonical public-only synthetic GPG export. Production key validation is unchanged. The existing retired-Danse instruction repair was brought in from main; do not recreate the retired application tree. Exact #2569 remote CI passed 6,842 CLI tests, 4 skipped, and 48 API tests. This does not replace the required local heavy/build/deploy predicates.
2. Estate configuration/evidence: malformed and missing registrations remain visible; duplicate JSON fields are rejected; installed plugin version/project scope and overridden provenance are retained. Claude default settings/plugin roots and OpenCode environment handling were corrected. Native receipts now require matching effective launch identity, fresh run/session/version/dependency bindings and independently collected observations. Caller receipt bundles cannot authenticate themselves.
3. Native Codex: installed app-server protocol collector, native catalog, paginated MCP status, effective configuration fingerprints, fresh ephemeral threads, and owner-declared read-only tool calls through `mcpServer/tool/call`. Dependency and route validation precede functional calls. No model turn/login is sent; response content is excluded. Rendered skill-budget evidence remains missing.
4. Native OpenCode: installed 1.18.20 HTTP API observer, live API/version checks, effective configuration, owned native session, MCP status and conditional owned-session/process cleanup. Its MCP status lacks a bound server version, so it cannot yet mint a passing registration receipt. It is not a passing live canary.
5. Gateway: strict declared/materialized configuration comparison, missing/extra/disabled upstream accounting, and capability/schema comparison under explicit route maps. Gateway coverage gaps affect estate exit. Complete live native-route equivalence and cutover authorization remain unfinished.
6. Process custody: sampled process/RSS evidence, PID-reuse-resistant witnessed-child handling, and bounded process-group absence verification. Unknown groups are never signalled from UID or stale PID alone.
7. Domus: private write-ahead repair custody plus target/policy/version-bound conditional rollback; concurrent/interrupted repairs preserve custody and refuse unsafe replay. No live configuration was applied.

Test receipts are separate scoped batches, not one full final suite: Domus 58 focused tests and 20 ownership declarations; gateway 16 tests; process/protocol/Codex 60 affected tests; OpenCode 12 tests; effective-configuration/collector batch 82 tests; latest changed native/configuration shard 33 tests. Both subsequent scoped cheap batches passed 11/11 gates. See the continuation for chronology and retained earlier receipts. Required heavy gates and live canaries remain unproven.

### Execution state and bootstrap

The direct human-protected Codex session is `mcp-estate-complete-20260908`. The most recent execution was run `run-838daff04c181b2d3d933b9d4bbcc455`, lease `lease-1041-838daff04c181b2d`, deadline `2026-09-08T23:40:02.178Z`. It was active at handoff preparation. Inspect its current broker graph before claiming or mutating; the outgoing session should submit a partial handoff receipt and release its lease. Never reuse another session's capability token or assume the lease remains active.

Use the documented credential bootstrap in `scripts/lib/workstream-capsule.sh`: `workstream_hydrate_conduct_environment`. Register the actual native session as human-protected and claim isolated branch/worktree/affected paths through the broker. Private runtime helpers are under `.limen-private/mcp-estate-complete-20260908`; token-bearing claim files must never be printed or committed. Do not edit `tasks.yaml` or manually restore publication refs. No native subagent fanout was used.

### Next actions and outstanding scope

The next source task selected before interruption was persistent healing orchestration: an owned failure episode permits one registered repair and one verification, including concurrent/interrupted runs. Only the existing Domus implementation was inspected; **no new healing-orchestrator code was written**. Continue this admissible lane, reusing existing monitor/update hooks and preserving private write-ahead state.

Continue the other missing work from the original plan: remaining native adapter producers/witnesses (ChatGPT, Claude Code/Desktop, Gemini, Agy, Copilot, Cline, Cursor, VS Code, hosted connectors and ianva); complete actual-client environment/loader/plugin/project resolution; broader atomic owner modifiers; native skill rendering/budget telemetry; public status consistency; full gateway protocol/capability/routing equivalence; and fresh startup/UI/dashboard/isolation/resource/cleanup canaries. A producer scaffold, synthetic test or direct server probe does not certify a client route.

At `2026-09-08T22:21:18Z`, local heavy admission remained denied for `swap-fraction,vitals-shed`, swap fraction `0.39838`, with no live host lease. Reconsider heavy execution only against changed observed conditions; do not bypass admission or stop independent source work. Required #2569 local build/deploy verification is still missing despite green remote CI.

Finish #2569's projection-recovery and isolation predicates, then merge its verified exact head through Limen's declared fast rail. Deploy the Worker using the captured merged main SHA, verify runtime identity, and prove two consecutive real task mutations without manual publication-ref restoration. The prior canonical task-context update returned busy against this own scoped lease because the deployed broker added repository-wide exclusion; the full sanitized response and owner are in the continuation. Completion requirements and the operator's no-premature-stop correction are durable in the execution packet and continuation, but the canonical task-context update has not succeeded.

Preserve LaunchDarkly's direct hosted-OAuth route. Last read-only status was not logged in. Consent remains with `L-LAUNCHDARKLY-OAUTH-CONSENT`; health checks must not initiate login. Do not treat credential consent as permission to block unrelated implementation.

Once eligible, land Limen through its exact-head fast rail. Submit Domus once through its prescribed rail, obtain positive landing proof, then apply only targeted configurations with rollback references. Deploy exact revisions, stage only equivalence-proven future-session gateway routes, preserve active sessions, collect deployment/runtime receipts, and run an unfiltered post-deployment estate measurement.

### Completion predicate — unchanged

Prove separately: (1) all required implementation regression/scoped predicates pass; (2) exact source revisions and live configurations have deployment receipts; (3) every required registration has fresh passing evidence, both service and registration denominators are explicit, and all seven counts are zero: missing capabilities, ownership conflicts, unintended launches, protocol failures, authentication gaps, unmeasured integrations, abandoned processes.

The last inventory-only snapshot was 123 service identities and 156 registrations with 155 unmeasured integrations; it is neither current post-deployment evidence nor a passing estate measurement. All three completion outcomes remain unproven. Keep the full goal active; never substitute an open PR, partial receipt, queued merge or filed blocker for completion.
