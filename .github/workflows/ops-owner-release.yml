name: Ops-owned scheduler release carrier

on:
  push:
    branches: [main]
    paths: ['.github/workflows/ops-owner-release.yml']
  workflow_dispatch:

permissions:
  contents: read

# Owner preflight preserves product authorization and dormant source custody.
jobs:
  owner:
    uses: 4444J99/ops/.github/workflows/cloudflare-owner-release.yml@76f10d2e870cc2c1620f934673598c60e5ebd1c2
    with:
      source_sha: 76f10d2e870cc2c1620f934673598c60e5ebd1c2
      apply_source: 76f10d2e870cc2c1620f934673598c60e5ebd1c2
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
