# Runtime lag unknown result

An installed runtime whose source cannot be measured printed UNVERIFIED but returned zero. Return 77 for that branch, preserving known absent-install and measured-current behavior. The observer must not mistake missing evidence for freshness. Verify the CLI exit status and machine-readable output; runtime adoption remains separate.
