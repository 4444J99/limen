# Current host gauge observation

Owner: Limen #2664. The immutable heartbeat must not require the retired fast-wave
sampler's mutable logs. Add an explicitly read-only on-demand mode using the
existing host-admission sensors; preserve the legacy maintenance diagnostic.
Current gauge evidence is not permission to launch or proof of low pressure.
Malformed, partial, stale, future or failed measurements remain unmeasured.
The existing 15-second observer deadline bounds execution and cleanup.

Verify focused gauge tests and scoped gates, then deploy the accepted immutable
source and bind actual scheduled probe evidence separately.
