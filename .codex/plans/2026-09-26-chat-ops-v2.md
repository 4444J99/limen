# Chat operations acceptance checkpoint

Owner: Codex. Portfolio owner: 4444J99/limen#2680; credentials: #320.

## Completed with receipts

- Approved OAuth fix PR #2746 merged at
  `ccc0c7ac0262b75bb8beb056796b7e98bc283b99`.
- Exact-main deployment succeeded: Actions run `36205964669`.
- Existing ChatGPT plugin now discovers fourteen tools (four read, ten write).
  Actual read-only capabilities call returned that exact SHA and deployment
  `104a1db9-2790-49b4-b938-5e9ffcad7c82`.
  Chat receipt: https://chatgpt.com/c/6ab71635-8408-83e9-89a5-e3aff79a0363
- Approved recovery reused the existing Jules key, with no mint or rotation.
  Limen Actions now has JULES_API_KEY. Canonical value custody is in the existing
  Limen-Automation vault with an explicit concealed JULES_API_KEY field.
  Values never entered source or receipts. Transient browser copy was cleared.
- Authenticated read from that custody enumerated 259 sources across three
  pages. This proves credential validity, not full account reconciliation.

## Discovered engineering defect and bounded candidate

Protected account-readback run `36206039102`, artifact `10893841490`, failed
with `response_limit_exceeded`. The large account exceeds the existing 4 MB
per-response bound at pageSize 100. A candidate now halves only oversized GET
pages at the same cursor; it retains the byte ceiling, whole-catalog deadline,
successful-page ceiling and single-record fail-closed behavior. At most six
reductions are possible across one catalog. It does not retry mutations or
provider rejection and does not publish partial counts as complete.

All 49 focused REST tests pass. The scoped batch passed type checking and
the other cheap gates but failed the whole-estate Ruff gate on inherited
`apps/vision-board-studio/photos-restore/1_embed_library.py` findings; heavy
gates did not run. The candidate live session read subsequently returned
`transport_unavailable`, so it is NOT a demonstrated complete repair.

Next owner action: distinguish one oversized session from aggregate-page
pressure and request the smallest documented session projection needed for
account observation, retaining full records for execution reconciliation.
Verify complete pagination, deadlines, byte caps, and no duplicate starts.
Do not increase limits blindly or infer zero usage from unavailable data.

## Remaining acceptance

CLAVIS run `36206259762` preceded the explicit field-label correction and
reported no matching field. Current custody was independently verified after
that correction; repeat delivery only with the changed source and compatible
bounded reader. Its pinned old client still has the large-account defect.

Scheduled Jules occurrence, useful provider execution through the keeper,
exact-head output integration, and cross-repository write isolation remain
unproved under #2680. No coding-provider sessions were started by this attempt.
The candidate checkout is retained with its pushed PR for continued engineering.
