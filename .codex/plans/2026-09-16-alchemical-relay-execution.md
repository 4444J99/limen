# Existing-target relay execution preflight

Owner: Codex; full gap-filling six-target receipt work; admission owners #269/#1995.

Live GitHub identity checks confirm alchemical-synthesizer ID 1157553907 is public at default 8de2bd7bdbc075a8971e5de224bed5d020b0d8f4. Relay ID 1350979676 is public at b53a47f93ef861044eb5a3b888a4fc72801dc4cf. Its frozen registry already permits alchemical-smoke-release-node22-v1 with Python 3.11.16 and Node 22.23.2. No seventh target or regression enrollment is proposed.

The profile performs strict smoke validation and builds/checks both release archives. The workflow has explicit target/SHA/profile/provenance inputs, isolated test permissions, finite job deadlines, and a separate receipt publication job. A passing artifact alone does not establish successful ledger publication. The exact inputs are in the companion JSON; no run ID or execution receipt exists for this preflight.

Before launch, obtain the required broker capacity reservation under the existing inventory contract; do not bypass the paused expansion lane or place workflow dispatch outside the broker record. Once admitted, use the existing relay launcher with the recorded four inputs, bind the returned run to the reservation, and require completed test steps plus durable ledger publication. Recheck immutable target/relay identity if the defaults have moved; retain old observations rather than silently relabeling them current.

Keep relay #30's independent frozen-root promotion and issue #3's protected-governor canaries separate. Diagnostic execution cannot authorize merging trust roots or support an estate-health completion claim. No workflow dispatch, account change, protected setting or provider launch was performed in this increment.
