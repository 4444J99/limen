# HOSPES repository scope correction

Owner: Codex. The live public name resolves to a different immutable repository
than the historical operations campaign. Bind both identities separately; the
reused public name must not become an operations alias. Rekey private estate
classification to the observed private repository and retain both products.
Historical lever fields and broker task IDs remain unchanged; additive decision
evidence points to the current operations owner. No transfer or visibility
mutation was performed. Receipt: docs/receipts/hospes-repository-scope-20260916.json.

Next: finish public software PR #1; implement the missing private pilot acceptance
predicate in private custody; reconcile other live routing consumers from these
IDs. No actual participant or external acceptance outcome is claimed.
