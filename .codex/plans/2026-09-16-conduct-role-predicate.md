# Repeatable conduct role acceptance

Owner: dispatch, L-CONDUCT-REGISTRY-DEPLOY. Verify the exact complete installed
registry before probing each principal. Check capabilities access, exclusive
collector authority, and rejection of a deliberately absent observation. Bound
requests, reject redirects, cancel unused response bodies and emit counts only.
No valid inventory, provider launch, task transition, or credential mutation.

Hermetic tests cover expected boundaries, unexpected access, unavailable auth,
transport failure, and incorrectly accepted malformed observations. Live execution
requires the complete canonical credential configuration and remains separate
from source validation. Preserve prior eleven-principal live evidence.
