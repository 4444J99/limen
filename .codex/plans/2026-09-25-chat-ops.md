# Chat operations activation — 2026-09-25

Owner: Codex direct session; portfolio acceptance: 4444J99/limen#2680.
Credential custody: 4444J99/limen#320. Scheduled evidence: organvm-ci-relay#32.

## Approved outcome

Reuse the existing Limen plugin and canonical keeper. Chat submits intent; the
keeper owns engineering leases and receipts; ops owns runtime scheduling; each
target repository owns its code, tests, integration and deployment. The existing
five debt routines retain Jules as their delegated coding executor. Do not add
per-repository credentials, tenants, schedulers, or competing keepers.

## Verified observations

- The existing connected plugin has no discovered tools; refreshing receives
  HTTP 503 from the Worker OAuth path. Legacy authenticated MCP lists 14 tools.
- Cloudflare's runtime rejects fetch redirect mode `error` before making a
  request. Both the JWKS fetch and Chat GitHub client used that mode.
- Both clients now use `manual`; their existing status checks reject redirects
  without following their Location or forwarding credentials.
- All 36 focused OAuth, MCP and Chat GitHub tests pass, including the new
  GitHub redirect regression. Miniflare with the production compatibility date
  retrieves the existing issuer JWKS with HTTP 200 using `manual`.
- Scoped verification passed all five cheap gates. Its heavy wave was denied
  with exit 75 (`swap-fraction`). This is not a full-suite pass or merge receipt.

## Remaining acceptance, in order

1. Owner Codex: obtain the full scoped Worker verification receipt under host
   admission, or an explicitly authorized exact-head remote admission receipt;
   then merge through the protected PR rail and deploy the exact main SHA.
2. Refresh the existing plugin, prove real tool discovery and a read-only
   conduct capabilities call. An invalid-signature OAuth probe must return 401,
   not the current key-fetch 503. Do not create another OAuth account.
3. Resolve the existing Jules credential source through its credential owner;
   the names-only resolver currently returns `credential_source_unresolved`.
4. Correlate one authorized Chat request and one scheduled occurrence through
   keeper lease, actual Jules session, exact-head tests, PR, authorized merge
   and default-branch readback. A configured schedule is not execution proof.
5. Before fleet activation, prove repository numeric identity resolution,
   disjoint-repository concurrency and overlap denial through the same keeper.
   Remove any global Chat execution bottleneck only with these predicates;
   do not widen the existing canary credential to the whole estate.

The transport correction is a bounded prerequisite, not completion of these
portfolio predicates. Current shared boundary doctrine remains authoritative:
`docs/architecture/concurrent-integration.md`.
