# Closeout source coverage

Owner: L-CLOSEOUT-OBSERVE-ARM / this PR. Missing or unreadable session ledgers and malformed records are explicit source errors, suppress automatic remediation, and return unmeasured rather than empty success. Reports retain eligible/inspected/omitted counts when capped. Source diagnostics contain only fixed codes, not private content. Board custody failure handling and authoritative acceptance ingestion retain their existing independent requirements.
